<?php
/**
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Comment\Form\CommentsSearchForm $form
 * @var array $query
 */

$hasComments = !empty($query['has_comments']);

$form = $this->commentsSearchForm();
$form
    ->setData([
        'has_comments' => $hasComments,
    ]);

$element = $form->get('has_comments');
echo $this->formRow($element);
