<?php
/**
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\AbstractResourceEntityRepresentation $resource
 * @var bool $showSubscription
 * @var bool $showList
 * @var bool $showForm
 * @var string $label
 * @var string $structure "flat" (default) or "threaded"
 * @var bool $closedOnLoad
 * @var int $maxLength
 * @var bool $skipGravatar
 * @var string $legalText
 * @var null $parentId For threaded structure only. Null here.
 * @var array $options Options above as an array.
 */

if (empty($showSubscription) && empty($showList) && empty($showForm) ) {
    return;
}
?>

<div id="comments-container" class="comments-container">
    <?php if ($showSubscription): ?>
    <?= $this->commentSubscriptionButton($resource, $options ?? []) ?>
    <?php endif; ?>
    <?php if ($showList): ?>
    <?= $this->comments($resource, $options ?? []) ?>
    <?php endif; ?>
    <?php if ($showForm): ?>
    <?= $this->commentForm($resource, $options ?? []) ?>
    <?php endif; ?>
</div>
