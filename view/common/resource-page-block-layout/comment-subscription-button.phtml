<?php
/**
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\AbstractResourceRepresentation $resource
 */

// When used as a resource page block, the admin explicitly added
// it, so skip the site setting check.
echo $this->commentSubscriptionButton($resource, ['skipSettingCheck' => true]);
